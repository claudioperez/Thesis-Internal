% \chapter{Rotation Derivatives}
\hypertarget{the-exponential}{%
\subsection{Derivatives}\label{the-exponential}}

In this section, several formulas are presented that facilitate the use of calculus on the rotation manifold \(\mathrm{SO}(3)\). 
Resources for this material include \cite{barfoot2017state}, \cite{hertzberg2008framework}, \cite{iserles2000liegroup}.

\subsubsection{\texorpdfstring{\(\operatorname{Exp}\): The Exponential
on
\(\mathrm{SO}(3)\)}{Exp: The Exponential on SO(3)}}\label{sec:exp-so3}
%
\paragraph*{Definition}

The \emph{exponential} map
\(\exp\colon \mathfrak{so}(3) \rightarrow \mathrm{SO}(3)\) associates an
element of the Lie algebra to a rotation. 
The exponential of \(\bm{\Omega} \in \mathfrak{so}(3)\) is defined as
\(\exp \bm{\Omega} =\gamma(1)\) where %
\(
\gamma: \mathbb{R} \rightarrow \mathrm{SO}(3)
\)
solves the initial-value problem:
\[
  \left\{
  \begin{aligned}
    \dot{\gamma} - \bm{\Omega}\gamma &= \bm{0} \\
    \gamma(0) &= \bm{1}
  \end{aligned}
  \right.
\]
% is the unique one-parameter subgroup of \(\mathrm{SO}(3)\) whose
% tangent vector at the identity is equal to \(\bm{\Omega}\). 
This definition implies the alternative \emph{functional}
definition which requires the following identities hold:
%
\begin{equation*} 
\begin{aligned}
\exp((\xi+\varepsilon) \bm{\Omega}) & = \exp(\xi \bm{\Omega}) \, \exp(\varepsilon \bm{\Omega}), \\
\frac{\mathrm{d}}{\mathrm{d}\varepsilon} 
\left.\operatorname{exp}(\varepsilon \bm{\Omega})\right.\biggr|_{\varepsilon=0} & =\bm{\Omega} .
\end{aligned}
\end{equation*}
  for any \(\xi, \varepsilon \in \mathbb{R}\) and
\(\bm{\Omega} \in \mathfrak{so}(3)\).
The following capitalized notation
is used to express this as a function of vectors in
\(\mathbb{R}^3\):
%
\begin{equation*} 
  \operatorname{Exp} \bm{\theta} \triangleq \exp \bm{\theta}^{\times}
%\begin{aligned}
%\text{Exp}\colon \mathbb{R}^3  &\rightarrow \mathrm{SO}(3), \\
%\bm{\theta} &\mapsto \exp \left(\bm{\theta}^{\times}\right)
%\end{aligned}
%\qquad\text{ and }\qquad
%\begin{aligned}
%\operatorname{Log} \colon \mathrm{SO}(3) &\rightarrow \mathbb{R}^3 ,\\
%\bm{\Lambda} &\mapsto \log (\bm{\Lambda})^{\vee}.
%\end{aligned}
\end{equation*}
 

\paragraph*{Expression}

A closed-form expression for the exponential in \(\mathrm{SO}(3)\) is
furnished by the Rodrigues formula: 
\begin{equation*}
\operatorname{Exp} \bm{\theta} 
\;=\; \cos \theta \mathbf{1} +\frac{\sin \theta}{\theta} \bm{\theta}^{\times} +\frac{1-\cos \theta}{\theta^2} \bm{\theta} \otimes \bm{\theta}
\end{equation*}
where \(\theta \triangleq \|\bm{\theta}\|\).
\begin{equation}
\begin{aligned}
\bm{\Lambda}
&= \cos \theta \mathbf{1} +\frac{\sin \theta}{\theta} \bm{\theta}^{\times} +\frac{1-\cos \theta}{\theta^2} \bm{\theta} \otimes \bm{\theta} \\
&=\bm{1}+a_1 \bm{\theta}^{\times} + a_2 \left(\bm{\theta}^{\times}\right)^2 \\
&=a_0 \bm{1} + a_1 \bm{\theta}^{\times} + a_2 \bm{\theta}\otimes\bm{\theta} \\
&\equiv \operatorname{Exp} \bm{\theta}
\end{aligned}
\label{eq:ExpSO3}
\end{equation} 
Also:
\begin{equation*}
\exp [\Theta]=\bm{1}+\frac{2}{1+|\overline{\hat{\Theta}}|^2}\left(\bar{\bm{\Theta}}+\bar{\bm{\Theta}}^2\right), 
\quad \text { where } \quad 
\bar{\bm{\Theta}}\triangleq\frac{1}{2} \frac{\tan \left(\frac{1}{2}|\Theta|\right)}{\frac{1}{2}|\Theta|} \bm{\Theta}
\end{equation*}
\hypertarget{LogSO3}{%
\subsubsection{\texorpdfstring{\(\operatorname{Log}\): The Logarithm on
\(\mathrm{SO}(3)\)}{Log: The Logarithm on SO(3)}}\label{sec:LogSO3}}

\paragraph*{Definition}
The \emph{logarithm}
\(\log\colon\mathrm{SO}(3)\rightarrow\mathfrak{so}(3)\) associates a
rotation to a trajectory through the identity. When restricted to the open ball
\(\|\bm{\theta}\|<\pi\), the exponential is a bijection and the logarithm is
its inverse. However, if one does not restrict the domain, the
exponential becomes non-injective as every vector
\((\|\bm{\theta}\|+2 k \pi) \bm{\theta}\) will map to the same
\(\bm{\Lambda}\) for any integer \(k\).
The logarithm may be defined in terms of the infinite series:
\begin{equation*}
\begin{aligned}
%\log %\colon \mathrm{SO}(3) & \rightarrow T_{\bm{1}} \mathrm{SO}(3) \\
%  = -\sum_{k=1}^{\infty} \frac{1}{k}(\bm{1}-\bm{\theta})^k
\log \bm{\Lambda}=\sum_{k=1}^{\infty}(-1)^{k+1} \frac{(\bm{\Lambda}-\bm{1})^k}{k}
 \end{aligned}
\end{equation*}
The following capitalized notation
is used to express the logarithm as a function of vectors in
\(\mathbb{R}^3\) representing element of \(\mathfrak{so}(3)\):
\begin{equation*}
  \begin{aligned}
    \operatorname{Log}\bm{\Lambda} \triangleq \left(\log \bm{\Lambda}\right)^{\vee}
    %\operatorname{Log} \colon \mathrm{SO}(3) &\rightarrow \mathbb{R}^3 ,\\
    %\bm{\Lambda} &\mapsto \log (\bm{\Lambda})^{\vee}.
  \end{aligned}
\end{equation*}
where the notation \((~\cdot~)^{\vee}\) of Equation (\ref{eq:vee}) is employed.

\paragraph*{Expression}

A common formula for the logarithm in \(\mathrm{SO}(3)\)  is: %
\begin{equation*} 
\log \bm{\Lambda}=\frac{\vartheta \left(\bm{\Lambda}-\bm{\Lambda}^{\mathrm{t}}\right)}{2 \sin \vartheta} 
\qquad\text { with }\qquad
 \vartheta=\cos ^{-1}\left(\frac{\operatorname{tr}\bm{\Lambda}-1}{2}\right) .
\end{equation*}
which holds for \(\vartheta \neq 0\) and \(\vartheta \in(-\pi, \pi)\).
However, this expression is problematic when \(\vartheta\) is small and should not be used in practice. 
An ideal 
 procedure to compute the logarithm is the algorithm proposed by  \cite{spurrier1978comment},
 which is summarized in Algorithm~\ref{alg:LogSO3}.

\begin{algorithm}[!h]
\caption{\enskip Algorithm from \cite{spurrier1978comment} for computing the logarithm $\operatorname{Log}$ on $\mathrm{SO}(3)$.}%
\label{alg:LogSO3}
\begin{algorithmic}
  \Function{LogSO3}{$\bm{\Lambda}$}
  \Statex{\(\triangleright\) Form quaternion $(q_0 + \bm{q})$}

  \State {$a = \max\{\operatorname{tr}\bm{\Lambda},\, \Lambda_{1 1},\, \Lambda_{2 2},\, \Lambda_{3 3}\}$} 
  \If {$a \equiv \operatorname{tr}\bm{\Lambda}$}
    \State {$q_0 = \frac{1}{2}\sqrt{1+a}$}
    
    \For {i = 0:2}
      \State {$j = (i+1)\%3$}
      \State {$k = (i+2)\%3$}
      \State {$q_i = (\Lambda_{k j} - \Lambda_{j k})/(4 q_0)$}
    \EndFor 
  \Else 
    \State {$i = \operatorname{argmax}\{\operatorname{tr}\bm{\Lambda},\, \Lambda_{1 1},\, \Lambda_{2 2},\, \Lambda_{3 3}\}$}
    \State {$j = (i+1)\%3$}
    \State {$k = (i+2)\%3$} 

    \State {$q_i = \sqrt{a/2 + (1 - \operatorname{tr}\bm{\Lambda})/4}$}
    \State {$q_j = (\Lambda_{j i} + \Lambda_{i j})/(4 q_i)$}
    \State {$q_k = (\Lambda_{k i} + \Lambda_{i k})/(4 q_i)$}
    \State {$q_0 = (\Lambda_{k j} - \Lambda_{j k})/(4 q_i)$}
  \EndIf

  \State {$\bm{q} = q_1 \mathbf{i} + q_2 \mathbf{j} + q_3 \mathbf{k}$}

  \Statex{\(\triangleright\) Form vector $\bm{\theta}$}
    \State {$q = \|\bm{q}\|$}

    \If {$q_0 < 0$}
       \State {$q_0 = -q_0$}
       \State {$\bm{q} = -\bm{q}$}
    \EndIf

    \If {$q \equiv 0$}
       \State {$\bm{\theta} = \bm{0}$}
    \ElsIf {$q < q_0$}
       \State {$\bm{\theta} = (\bm{q}/q) 2 \sin^{-1}q$}
    \Else
       \State {$\bm{\theta} = (\bm{q}/q) 2 \cos^{-1}q_0$}
    \EndIf
  
\Return {$\bm{\theta}$}
  
\EndFunction
\end{algorithmic}
\end{algorithm}


\iffalse
\hypertarget{identities}{%
\subsubsection{Identities}\label{identities}}

The general matrix exponential has the property that for any \FCF{matrix} \(\bm{V}\) and any invertible \FCF{matrix} \(\bm{A}\) \FCF{it holds that}  %
\begin{equation*} 
\operatorname{exp} \left(\bm{V}^{\mathrm{t}}\right)=(\operatorname{exp} \bm{V})^{\mathrm{t}}
\qquad\text{ and }\qquad
\operatorname{exp} \left(\bm{A} \bm{V} \bm{A}^{-1}\right) =\bm{A} \operatorname{exp} \left(\bm{V}\right) \bm{A}^{-1}.
\end{equation*}
  Furthermore, a matrix exponential is \emph{always} invertible and one
has: %
\begin{equation*} 
(\operatorname{exp} \bm{V})^{-1}=\operatorname{exp} \left(-\bm{V}\right)
\qquad\text{ and }\qquad
\operatorname{det}\left(\operatorname{exp}\bm{V}\right)=e^{\operatorname{tr}(\bm{V})}
\end{equation*}
where the former is in fact a special case of the more general
identity %
\begin{equation*} 
\exp \left(\bm{V}\right)^\alpha=\exp \left(\alpha \bm{V}\right).
\end{equation*}
 \fi

 \subsubsection{Cay}

 \begin{equation}
 \operatorname{cay}[\bm{\zeta}^{\times}] = 
\bm{1}+\frac{1}{1+\zeta^2}(\mathbf{1}+(\bm{\zeta}^\times))\bm{\zeta}^\times
\end{equation}
$$
\zeta=\tan \frac{\varphi}{2} \mathbf{u}
$$
$$
\left(\frac{d}{d \Omega} \text { cay } \Omega\right) H=\left(d \operatorname{cay}_{\Omega}(H)\right) \text { cay } \Omega
$$
where
$$
d \text { cay } \Omega(H)=2(I-\Omega)^{-1} H(I+\Omega)^{-1}
$$
For the inverse of $d$ cay ${ }_{\Omega}$ we have

$$
d \operatorname{cay}_{\Omega}^{-1}(H)=\frac{1}{2}(I-\Omega) H(I+\Omega)
$$

\subsubsection{Ham}

\begin{equation}
\bm{R}=\left(\mathbf{I}+\frac{1}{q_0}(\bm{q} \times)\right)\left(\bm{1}+\frac{1}{q_0}(\bm{q} \times)\right)^{-T}
=\left(\bm{1}+\frac{1}{q_0}\bm{q}^\times\right)^{-T}\left(\bm{1}+\frac{1}{q_0}\bm{q}^\times\right)
\end{equation}


\hypertarget{sec:exponential-differentials}{%
\subsection{Exponential Differentials}\label{sec:exponential-differentials}}

The differential or push-forward
\(\operatorname{dexp}_{\bm{\Omega}}\) of the exponential \(\exp\) at
\(\bm{\Omega}\)
is defined by: %in the sense of Section~\ref{sec:push-forward}:
%follows by the standard procedure: %
\begin{equation*} 
\operatorname{dexp}_{\bm{\Omega}} \bm{W} 
  =\left.\frac{\mathrm{d}}{\mathrm{d} \varepsilon} \exp  {\gamma_{\varepsilon}}\right|_{\varepsilon=0}
\quad\text{ with }\quad
\begin{aligned}
\gamma(0)&=\bm{\Omega}, \text{ and }\\
\dot{\gamma}(0)&=\bm{W} .
\end{aligned}
\end{equation*}
%
Note that this furnishes a function with the signature: %
\begin{equation*} 
\operatorname{dexp}_{\bm{\Omega}} : T_{\bm{\Omega}} \mathfrak{so}(3) \rightarrow T_{\exp ({\bm{\Omega}})} \mathrm{SO}(3).
\end{equation*}
However, as indicated earlier, it is %will be
preferable to identify each
space \(T_{\exp (\bm{\Omega})} \mathrm{SO}(3)\) with the single Lie
algebra \(\mathfrak{so}(3)\). That is, for any rotation \(\bm{\Lambda}\), we seek a systematic way to associate a tangent \(\dot{\bm{\Lambda}} \, \in \, T_{\scriptscriptstyle{\Lambda}} \mathrm{SO}(3)\) with an element \(\bm{\Omega}\) of the Lie algebra \(\mathfrak{so}(3) \equiv T_{1} \mathrm{SO}(3)\).
There are two standard ways to make this
association:

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
% \tightlist
\item
  \emph{Left representation}:% Velocities \(\dot{\bm{\Lambda}}\) are associated with the unique skew operator \(\bm{\Psi}\) s%
  \begin{equation*} 
  %{ }_{\mathrm{mat}} 
  T^{r}_{\scriptscriptstyle{\Lambda}} \mathrm{SO}(3) \triangleq \left\{
  %\bm{\Psi}^{\times}_{\bm{\Lambda}} \triangleq (\bm{\Lambda}, \bm{\Psi}^{\times}) \mid \text { with } 
  \bm{\Psi}\text{ with }\dot{\bm{\Lambda}} \triangleq \bm{\Lambda} \bm{\Psi} \mid \bm{\Lambda} \in \mathrm{SO}(3), \bm{\Psi} \in \mathfrak{so}(3)\right\}
  \end{equation*}
 
\item
  \emph{Right representation}: %
\begin{equation}
    T^{\ell}_{\scriptscriptstyle{\Lambda}} \mathrm{SO}(3) \triangleq \left\{
   %\bm{\theta}^{\times}_{\bm{\Lambda}} \triangleq (\bm{\Lambda}, \bm{\theta}^{\times}) \mid \text { with } 
   \bm{\Theta}\text{ with }\dot{\bm{\Lambda}} \triangleq \bm{\Theta} \bm{\Lambda} \mid \bm{\Lambda} \in \mathrm{SO}(3), \bm{\Theta} \in \mathfrak{so}(3)\right\}
   \label{eq:rTSO3}
    \end{equation}
 
\end{enumerate}

For each of these representations, a \emph{trivialized} differential may
be constructed.
Furthermore, when \(\mathfrak{so}(3)\) is identified with \(\mathbb{R}^3\), these are denoted by
\(\operatorname{d_{\scriptscriptstyle{R}}Exp}\) and
\(\operatorname{d_{\scriptscriptstyle{L}}Exp}\) for the 
right and left
representations, respectively, and each carries the signature
\(\mathbb{R}^3 \times \mathbb{R}^3 \rightarrow \mathbb{R}^3\). 
For a curve \(\gamma: \mathbb{R}\rightarrow \mathfrak{so}(3)\) these are
defined by the relations: 
%
\begin{equation} 
\begin{aligned}
\frac{\mathrm{d}}{\mathrm{d} \varepsilon}
\operatorname{Exp} \gamma_{\varepsilon} 
& =\operatorname{d_{\scriptscriptstyle{R}}Exp}({\gamma_{\varepsilon}})\,\dot{\gamma}(\varepsilon) \,\operatorname{Exp} \gamma_{\varepsilon} \\
& =\operatorname{Exp} (\gamma_{\varepsilon})\, \operatorname{d_{\scriptscriptstyle{L}}Exp}({\gamma_{\varepsilon}}) \, \dot{\gamma}(\varepsilon).
\end{aligned}
\label{eq:def-dExpSO3}
\end{equation}
%
\paragraph*{Remarks}
\begin{itemize}
  \item In the literature on geometrically exact rods, \cite{ibrahimbegović1995computational,ritto-corrêa2002differentiation} use the notation \(\bm{T}\) for 
    \(\operatorname{d_{\scriptscriptstyle{R}}Exp}{\bm{\theta}}\).
      Alternatively, \cite{simo1988dynamics,jelenić1999geometrically} used \(\bm{T}\) to refer to  \(\left(\operatorname{d_{\scriptscriptstyle{R}}Exp}\bm{\theta}\right)^{-1}\) and \cite{simo1991unconditionally} uses the symbol for \(\left(\operatorname{d_{\scriptscriptstyle{L}}Exp}\bm{\theta}\right)^{-1}\).
    \cite{cardona1988beam} used the same symbol for \(\operatorname{d_{\scriptscriptstyle{L}}Exp}\bm{\theta}\).

\item The notation \(\operatorname{dexp}\) employed in this work is consistent with \cite{rossmann2006lie} and differs slightly from of \cite{varadarajan1984lie,iserles2000liegroup}, 
  who use the symbol to denote a \emph{trivialized} differential. 
    That is, in the adopted notation, the expression in terms of the adjoint action \(\operatorname{ad}\)
    is given by: 
    \[
      \operatorname{dexp}_X Y=\exp X \frac{1-\exp (-\operatorname{ad} X)}{\operatorname{ad} X} Y.
    \]
\end{itemize}

Before proceeding to the expressions for these functions, some
definitions are necessary.
Define coefficients \(b_i\) and \(c_i\): %
\begin{equation*} 
\begin{array}{ccc}
b_i \triangleq \dfrac{1}{\theta} \dfrac{\mathrm{d} a_i }{\mathrm{d} \theta}
&\quad\text{ so }\quad
\delta a_i =b_i \,\bm{\theta} \cdot \delta \bm{\theta},
&\text{ and} \\[1ex]
c_i \triangleq \dfrac{1}{\theta} \dfrac{\mathrm{d} b_i }{\mathrm{d} \theta}
&\quad\text{ so }\quad
\delta b_i = c_i \, \bm{\theta} \cdot \delta \bm{\theta}.
\end{array}
\end{equation*}
where \(a_i\) are the the coefficients in Equation~(\ref{eq:ExpSO3}) for the
exponential map, \(\bm{\theta}\) its argument, and \(\theta \triangleq \|\bm{\theta}\|\). In closed form the coefficients \(b_i\) and \(c_i\) are:

\begin{equation}
\begin{array}{lccc}
i & a_i & b_i & c_i \\[1ex] \hline \\[0.1ex]
0 & \cos \theta 
  & -\dfrac{\sin \theta}{\theta}
  & \dfrac{\sin \theta-\theta \cos \theta}{\theta^3}
\\[0.3cm] 
1 & \dfrac{\sin \theta}{\theta}
  & \dfrac{\theta \cos \theta-\sin \theta}{\theta^3}
  & \dfrac{3 \sin \theta-\theta^2 \sin \theta-3 \theta \cos \theta}{\theta^5}
\\[0.3cm]
2 & \dfrac{1-\cos \theta}{\theta^2} %= \dfrac{1}{2} \dfrac{\sin (\theta / 2)}{(\theta / 2)^2}  
  & \dfrac{\theta \sin \theta-2+2 \cos \theta}{\theta^4}
  & \dfrac{8-8 \cos \theta-5 \theta \sin \theta+\theta^2 \cos \theta}{\theta^6} 
\\[0.3cm]
3 & \dfrac{\theta-\sin \theta}{\theta^3} 
  & \dfrac{3 \sin \theta-2 \theta-\theta \cos \theta}{\theta^5} 
  & \dfrac{8 \theta+7 \theta \cos \theta+\theta^2 \sin \theta-15 \sin \theta}{\theta^7}
\end{array}
\label{eq:gib}\end{equation}
Many of these equations are singular at \(\theta = 0\).
Near this condition, the following series
expression should be used \citep{ritto-corrêa2002differentiation}.

\begin{equation}
z_{j i}=\sum_{k=0}^{\infty} \frac{2^j(j+k) !(-1)^{k+j} \theta^{2 k}}{k !(i+2 j+2 k) !}= \begin{cases}a_i(\theta) & \text { if } j=0 \\ b_i(\theta) & \text { if } j=1 \\ c_i(\theta) & \text { if } j=2\end{cases}
\label{eq:gib-series}\end{equation}
Remarkably, this expression contains
every coefficient in Equation~(\ref{eq:gib}).
It is useful to define a similar table for %from
the coefficients of the inverse exponential differential:

\begin{equation}
\begin{array}{lccc}
i & \alpha_i & & \beta_i \\ \hline \\[0.1ex]
0 & 1 + \dfrac{1}{2}\theta^2
  &
  & % 1
\\[0.3cm]
1 & \dfrac{\theta / 2}{\tan (\theta / 2)}
  &
  & 
\\[0.3cm] 
2 & -\dfrac{1}{2}
  &
  & % 0
\\[0.3cm]
3 & \dfrac{1}{\theta^2}\left(1 - \dfrac{\theta / 2}{\tan (\theta / 2)}\right) 
  & %\dfrac{\sin \left(\frac{\theta}{2}\right)-\frac\theta{2} \cos \left(\frac{\theta}{2}\right)}{\theta^2 \sin{\frac{\theta}{2}}}
  & \dfrac{\theta (\theta+\sin\theta) -8 \sin \left(\frac{\theta}{2}\right)^2}{4 \theta^4 \sin^2\frac{\theta}{2}}
\\[0.3cm]
\end{array}
\label{eq:ber}
\end{equation}

\hypertarget{sec:drExpSO3}{%
\subsubsection{\texorpdfstring{\(\operatorname{d_{\scriptscriptstyle{R}}Exp}\)
: Right Differential of the Exponential}{drExp : Right Differential of the Exponential}}\label{sec:drExpSO3}}
%

\paragraph*{Definition}
%
The \emph{right-trivialized differential} of the exponential \(\operatorname{d_{\scriptscriptstyle{R}}Exp} {\bm{\theta}}\) is a function that 
maps variations of the argument \(\bm{\theta}\)
into right-represented vectors in the tangent space. An extensive discussion is
provided by \cite{ibrahimbegović1995computational}. Interesting
treatments from alternative perspectives can be found in
\cite{pietraszkiewicz1983finite,simo1986threedimensional,pfister1998bernoulli}. Note that this function is sometimes referred to as the ``left Jacobian'' of \(\mathrm{SO}(3)\).
%
The right-trivialized differential is defined by Equation~(\ref{eq:def-dExpSO3}), 
and has been expressed by the limit \citep{sola2018lie}:
%
\begin{equation*} 
\begin{aligned}
\operatorname{d_{\scriptscriptstyle{R}}Exp}({\bm{\theta}})\,
\bm{u}
&\triangleq \lim _{\varepsilon \rightarrow 0} \frac{\operatorname{Log} \left(\operatorname{Exp}(\bm{\theta} + \varepsilon\bm{u}) \, \operatorname{Exp}(\bm{\theta})^{-1}\right)}{\varepsilon} \\
& =\left.\frac{\mathrm{d}}{\mathrm{d} \varepsilon}\operatorname{Exp} (\bm{\theta}+\varepsilon  \bm{u}) \operatorname{Exp} (-{\bm{\theta}})\right|_{\varepsilon=0}^{\vee}
\end{aligned}
\end{equation*}
 
\paragraph*{Expression}

In terms of the coefficients \(a_i\) in Equation~(\ref{eq:gib})
the function has the expression:

\begin{equation}
\begin{aligned}
\operatorname{d_{\scriptscriptstyle{R}}Exp} {\bm{\theta}}
%&=\frac{\sin \theta}{\theta} \bm{1}+\frac{1-\cos \theta}{\theta^2} \bm{\theta}^{\times} + \frac{\theta-\sin \theta}{\theta^3} \bm{\theta} \otimes\bm{\theta}\\
%&=\bm{1}+\frac{1-\cos \theta}{\theta^2} \bm{\theta}^{\times} + \frac{\theta-\sin \theta}{\theta^3} \left(\bm{\theta}^{\times}\right)^2 \\
 %&=a_1 \bm{1}+a_2 \bm{\theta}^{\times} + a_3 \bm{\theta} \otimes \bm{\theta} \\
 &=\bm{1}+a_2 \bm{\theta}^{\times} + a_3 \left(\bm{\theta}^{\times}\right)^2 \\
 %&=\int_0^1 \bm{\Lambda}^\alpha d \alpha \\
 %&=\sum_{n=0}^{\infty} \frac{1}{(n+1) !}\left(\phi^{\times}\right)^n \\
 %&=\frac{\sin \theta}{\theta} \bm{1}  +\frac{1-\cos \theta}{\theta} \bm{\theta}^{\times}  +\left(1-\frac{\sin \theta}{\theta}\right) \bm{\theta} \bm{\theta}^{\mathrm{t}} \\
\end{aligned}
\label{eq:drExpSO3}
\end{equation}
In \cite{simo1986threedimensional} the notation \(\bm{\beta}=\left(
\operatorname{d_{\scriptscriptstyle{R}}Exp} {\bm{\theta}}
\right)
\dot{\bm{\theta}}\) is used.

\hypertarget{dlExpSO3}{%
\subsubsection{\texorpdfstring{\(\operatorname{d_{\scriptscriptstyle{L}}Exp}\):
Left Differential of the
Exponential}{dlExp: Left Differential of the Exponential}}\label{sec:dlExpSO3}}

% The \emph{left-trivialized differential} of the exponential \(\operatorname{d_{\scriptscriptstyle{L}}Exp} {\bm{\theta}}\) maps variations of the argument \(\bm{\theta}\)
% into left-variations in the tangent space at
% \(\operatorname{Exp} \bm{\theta}\). 


\paragraph*{Definition}
The \emph{left-trivialized differential of the exponential} is defined by Equation~(\ref{eq:def-dExpSO3}), 
and has been expressed by the limit  \citep{sola2018lie}:
%
\begin{equation*} 
\begin{aligned}
\operatorname{d_{\scriptscriptstyle{L}}Exp} ({\bm{\theta}}) \, \bm{u}
&\triangleq
%{ }_{\mathrm{mat}} T_{\bm{1}} \mathrm{S O}(3) &\rightarrow{ }_{\mathrm{mat}} T_{\bm{\Lambda}} \mathrm{S O}(3) \\
 \lim _{\varepsilon \rightarrow 0} \frac{\operatorname{Log} \left(\operatorname{Exp}(\bm{\theta})^{-1} \, \operatorname{Exp}(\bm{\theta}+\varepsilon\bm{u})\right)}{\varepsilon} \\
& =\left.\frac{\mathrm{d}}{\mathrm{d} \varepsilon}\operatorname{Exp} (-{\bm{\theta}})\operatorname{Exp} (\bm{\theta}+\varepsilon  \bm{u})\right|_{\varepsilon=0}^{\vee}
\end{aligned}
\end{equation*}
 

\paragraph*{Expression}

In terms of the coefficients \(a_i\) in Equation~(\ref{eq:gib})
the function takes the following form:

\begin{equation}
\begin{aligned}
\operatorname{d_{\scriptscriptstyle{L}}Exp}{\bm{\theta}} &=\bm{1} - a_2 \bm{\theta}^{\times} + a_3 \left(\bm{\theta}^{\times}\right)^2 \\
%&= a_1 \bm{1}+\left(1-\frac{\sin \phi}{\phi}\right) \bm{\theta}\otimes\bm{\theta} -\frac{1-\cos \phi}{\phi} \bm{\theta}^{\times}
% \mathbf{T}_{\scriptscriptstyle{L}}(\bm{\theta}) & =\bm{1}-\frac{1-\cos \theta}{\theta^2}[\bm{\theta}]_{\times}+\frac{\theta-\sin \theta}{\theta^3}[\bm{\theta}]_{\times}^2 \\
% &= \frac{\sin \phi}{\phi} \bm{1}+\left(1-\frac{\sin \phi}{\phi}\right) \bm{\theta}\otimes\bm{\theta}-\frac{1-\cos \phi}{\phi} \bm{\theta}^{\times} \\
%&=\sum_{n=0}^{\infty}  \frac{1}{(n+1) !}\left(-\bm{\theta}^{\times}\right)^n \\
%&=\int_0^1 \bm{\Lambda}^{-\alpha} d \alpha \\
\end{aligned}
\label{eq:RightTanSO3}
\end{equation}
This expression is well-known and may be derived from Equation~(\ref{eq:drExpSO3}) using the identity \(\operatorname{d_{\scriptscriptstyle{R}}Exp}{(\bm{\theta})} = \operatorname{d_{\scriptscriptstyle{L}}Exp}{(-\bm{\theta})}\).

\hypertarget{sec:ddrExpSO3}{%
\subsubsection{\texorpdfstring{\(\operatorname{d^2_{\scriptscriptstyle{R}}Exp}\)
: Derivative of the Right Exponential
Differential}{ddrExpSO3 : Derivative of the Right Exponential Differential}}\label{sec:ddrExpSO3}}

\paragraph*{Definition}
The \emph{derivative of the right exponential differential} arises by linearizing expressions containing the right differential (Equation~\ref{eq:drExpSO3}).
It is defined %in the sense of Equation~(\ref{eq:dlinear})
as the directional derivative of the vector-valued function \(\bm{\theta}\mapsto \operatorname{d_{\scriptscriptstyle{R}}Exp}(\bm{\theta})\,\bm{p}\)
for some constant vector \(\bm{p}\):
%
\begin{equation*} 
%\bm{\Xi}_\ell(\bm{\theta},\bm{p}) 
\operatorname{d^2_{\scriptscriptstyle{R}}Exp}(\bm{\theta}; \, \bm{p}) \, \bm{u} 
  \triangleq \left.\frac{\mathrm{d}}{\mathrm{d} \varepsilon}\left.\operatorname{d_{\scriptscriptstyle{R}}Exp}({\gamma_{\varepsilon}})\right. \, \bm{p}\right|_{\varepsilon=0} 
\qquad\text{ with }\qquad
\begin{aligned}
\gamma_{\varepsilon} &= \bm{\theta} + \varepsilon \bm{u} \\
\dot{\gamma}(0) &= \bm{u}
\end{aligned}
\end{equation*}
 

\paragraph*{Expression}

In terms of the coefficients \(a_i\) and \(b_i\) in Equation~(\ref{eq:gib})
the function takes the following form:

\begin{equation}
\begin{aligned}
%\bm{\Xi}_\ell\left(\bm{\theta},\bm{p}\right) \bm{u} =& D [\bm{T}(\bm{\theta}) \bm{p}] \cdot \bm{u}\\
%\bm{\Xi}_\ell(\bm{p})
\operatorname{d^2_{\scriptscriptstyle{R}}Exp}(\bm{\theta}; \, \bm{p})
%&= \left[b_1 \bm{p}+b_2(\bm{\theta} \times \bm{p})+b_3\left(\bm{p} \cdot \bm{\theta}\right) \bm{\theta}\right] \otimes \bm{\theta}  -a_2\bm{p}^{\times} + a_3[\bm{\theta} \otimes \bm{p}+\left(\bm{p} \cdot \bm{\theta}\right) \bm{1}] \\
&=-a_2 \bm{p}^{\times}+a_3\left(\bm{p} \cdot \bm{\theta}\right) \bm{1}+a_3 \bm{\theta} \otimes \bm{p}
 + b_1 \bm{p} \otimes \bm{\theta}
 +b_2 \left(\bm{\theta}\times \bm{p}\right) \otimes \bm{\theta} 
 +b_3\left(\bm{p} \cdot \bm{\theta}\right) \bm{\theta} \otimes \bm{\theta} \\
\end{aligned}
\label{eq:ddlExpSO3}
\end{equation}
This expression has previously been derived by \cite{cardona1988beam}.

%
%
%
\hypertarget{sec:ddlExpSO3}{%
\subsubsection{\texorpdfstring{\(\operatorname{d^2_{\scriptscriptstyle{L}}Exp}\)
: Derivative of the Left Exponential
Differential}{ddlExpSO3 : Derivative of the Left Exponential Differential}}\label{sec:ddlExpSO3}}

\paragraph*{Definition}

The \emph{derivative of the left exponential differential} arises by linearizing expressions containing the left differential (Equation~\ref{eq:drExpSO3}).
It is defined %in the sense of Equation~(\ref{eq:dlinear}) 
as the directional derivative of the vector-valued function \(\bm{\theta}\mapsto \operatorname{d_{\scriptscriptstyle{L}}Exp}(\bm{\theta})\,\bm{p}\) 
for some constant vector \(\bm{p}\):
%
\begin{equation*} 
\operatorname{d^2_{\scriptscriptstyle{L}}Exp}({\bm{\theta}}; \, \bm{p}) \, \bm{u} 
  \triangleq \left.\frac{\mathrm{d}}{\mathrm{d} \varepsilon}\left.\operatorname{d_{\scriptscriptstyle{L}}Exp} ({\gamma_{\varepsilon}})\right. \bm{p}\,\right|_{\varepsilon=0}
,\qquad\text{ with }\qquad
\begin{aligned}
\gamma_{\varepsilon} &= \bm{\theta} + \varepsilon \bm{u} \\
\dot{\gamma}(0) &= \bm{u}
\end{aligned}
\end{equation*}
 
\paragraph*{Expression}

In terms of the coefficients \(a_i\) and \(b_i\) in Equation~(\ref{eq:gib})
the function takes the following form:

\begin{equation}
\begin{aligned}
\operatorname{d^2_{\scriptscriptstyle{L}}Exp}(\bm{\theta}; \bm{p})
% &=\left[b_1 \bm{p}-c_2\left(\bm{\theta} \times \bm{p}\right)
%  +c_3\left(\bm{\theta} \cdot \bm{p}\right) \bm{\theta}\right] \otimes \bm{\theta}
%  +a_2 \bm{p}^{\times} + a_3\left(\left(\bm{\theta} \cdot \bm{p}\right)[\bm{1}]+\bm{\theta} \otimes \bm{p}\right) \\
&=
 a_2 \bm{p}^{\times}
 +a_3\left(\bm{p} \cdot \bm{\theta}\right)\bm{1}
 +a_3\left.\bm{\theta} \otimes \bm{p}\right.
 +b_1 \left.\bm{p} \otimes \bm{\theta}\right.
 -b_2 \left.(\bm{\theta} \times \bm{p}) \otimes \bm{\theta}\right.
 +b_3 \left(\bm{p} \cdot \bm{\theta}\right)\left.\bm{\theta} \otimes \bm{\theta}\right.
\end{aligned}
\label{eq:ddrExpSO3}\end{equation}
%
This formula has been derived by \cite{jelenić1998interpolation} and
\cite{ritto-corrêa2002differentiation}.

\hypertarget{sec:dddExpSO3}{%
\subsubsection{\texorpdfstring{\(\operatorname{d^3Exp}\): Second
Derivative of the Left Exponential
Differential}{dddExpSO3: Second Derivative of the Left Exponential Differential}}\label{sec:dddExpSO3}}

\paragraph*{Definition}
The \emph{second derivative of the left exponential differential} arises from the linearization
of products with the first derivative of the left differential. 
It is defined in the sense of Equation~(\ref{eq:dlinear}):

%
\begin{equation*} 
\operatorname{d^3_{\scriptscriptstyle{R}}Exp}\left(\bm{\theta}; \, \bm{p}, \bm{q}\right) \, \bm{u}
\triangleq \left.\frac{\mathrm{d}}{\mathrm{d}\varepsilon}\operatorname{d^2_{\scriptscriptstyle{R}}Exp}\left(\gamma_{\varepsilon}; \, \bm{p}\right) \, \bm{q} \,\right|_{\varepsilon=0} 
,\qquad\text{ with }\qquad
\begin{aligned}
\gamma_{\varepsilon} &= \bm{\theta} + \varepsilon \bm{u} \\
\dot{\gamma}(0) &= \bm{u}
\end{aligned}
\end{equation*}
 

\paragraph*{Expression}

In terms of the coefficients \(a_i, b_i\) and \(c_i\) in Equation~(\ref{eq:gib})
the function takes the form:

\begin{equation}
\begin{aligned}
%\bm{\Xi}_{D^2 \mathbf{T}}(\bm{p}, \bm{q})
\operatorname{d^3_{\scriptscriptstyle{R}}Exp}\left(\bm{\theta};\bm{p}, \bm{q}\right)
= 
   & a_3(\bm{p} \otimes \bm{q}+\bm{q} \otimes \bm{p})
   + b_1(\bm{p} \cdot \bm{q}) \bm{1}
   + b_2(\bm{p}\times \bm{q} \otimes \bm{\theta}+\bm{\theta} \otimes \bm{p}\times\bm{q}+(\bm{\theta}\times \bm{p} \cdot \bm{q}) \bm{1}) \\
& +b_3\bigl(\bm{p} \cdot \bm{\theta}(\bm{q} \otimes \bm{\theta}+\bm{\theta} \otimes \bm{q}) + \bm{q} \cdot \bm{\theta} (\bm{p} \otimes \bm{\theta}+\bm{\theta} \otimes \bm{p})+\left(\bm{p} \cdot \bm{\theta}\right)(\bm{\theta} \cdot \bm{q}) \bm{1}\bigr) \\
& +\bigl(
    c_1 ~ \bm{p} \cdot \bm{q}
   +c_2(\bm{q}\times \bm{p}) \cdot \bm{\theta}
   +c_3\left(\bm{p} \cdot \bm{\theta}\right)(\bm{\theta} \cdot \bm{q})
  \bigr) \bm{\theta} \otimes \bm{\theta}
\end{aligned}
\label{eq:dddExpSO3}\end{equation}
%
This expression has been derived by
\cite{ibrahimbegović1995computational} and
\cite{ritto-corrêa2002differentiation}.

\hypertarget{drLogSO3}{%
\subsubsection{\texorpdfstring{\(\operatorname{d_{\scriptscriptstyle{R}}Log}\)
: Right Differential of the Logarithm
}{drLog : Right Differential of the Logarithm }}\label{sec:drLogSO3}}

\paragraph*{Definition}
The \emph{right differnetial of the logarithm} inverts the right exponential differential Equation (\ref{eq:drExpSO3}) and has been expressed as:
%
\begin{equation*} 
\begin{aligned}
\operatorname{d_{\scriptscriptstyle{R}}Log} \left({\bm{\theta}}\right) \, \bm{u}
  &= \left.\frac{\mathrm{d}}{\mathrm{d}\varepsilon} \operatorname{Log} \bigl( \operatorname{Exp} (\varepsilon \bm{u}) \operatorname{Exp} \bm{\theta}\bigr) \right|_{\varepsilon=0}
\end{aligned}
\end{equation*}

\paragraph*{Expression}
%
In terms of the coefficients \(\alpha_i\) in Equation~(\ref{eq:ber})
the function takes the following form:

\begin{equation}
\begin{aligned}
\operatorname{d_{\scriptscriptstyle{R}}Log} {\bm{\theta}}
%&=\bm{1} + \alpha_2 \bm{\theta}^{\times} + \alpha_3 \left(\bm{\theta}^{\times}\right)^2 \\
&=\alpha_1\bm{1} + \alpha_2\, \bm{\theta}^{\times} + \alpha_3\, \bm{\theta}\otimes\bm{\theta} \\
%&= \left[\operatorname{d_{\scriptscriptstyle{R}}Exp}_{\bm{\theta}}\right]^{-1}\\
%&=\mathbf{T}_{\scriptscriptstyle{R}}^{-1}(\bm{\theta})
\end{aligned}
\label{eq:drLogSO3}
\end{equation}
%
Derivations of this function can be found in \cite{simo1988dynamics}, 
\cite{nour-omid1991finite}, \cite{ibrahimbegović1995computational},
and \cite{bauchau2003vectorial}.

\hypertarget{sec:dlLog}{%
\subsubsection{\texorpdfstring{\(\operatorname{d_{\scriptscriptstyle{L}}Log}\):
Left Differential of the
Logarithm}{dlLog: Left Differential of the Logarithm}}\label{sec:dlLog}}

\paragraph*{Definition}
The \emph{left differential of the logarithm} is adjoint to the right differential of the logarithm Equation (\ref{eq:drLogSO3}), and has been expressed as:
%
\begin{equation*} 
\begin{aligned}
\operatorname{d_{\scriptscriptstyle{L}}Log} \left({\bm{\theta}} \right)\, \bm{u}
&= \left.\frac{\mathrm{d}}{\mathrm{d}\varepsilon} \operatorname{Log} \bigl(\operatorname{Exp} \bm{\theta}  \operatorname{Exp} (\varepsilon \bm{u})\bigr) \right|_{\varepsilon=0} \\
\end{aligned}
\end{equation*}
 

\paragraph*{Expression}

In terms of the coefficients \(\alpha_i\) in Equation~(\ref{eq:ber})
the function takes the following form:
\begin{equation*} 
\begin{aligned}
\operatorname{d_{\scriptscriptstyle{L}}Log} {\bm{\theta}}
&= \alpha_1 \bm{1}
  -\alpha_2 \bm{\theta}^{\times}
  +\alpha_3 \bm{\theta} \otimes \bm{\theta} \\
% &= \bm{1} - \alpha_2 \bm{\theta}^{\times} + \left(\frac{1}{\theta^2}-\frac{1+\cos \theta}{2 \theta \sin \theta}\right)[\bm{\theta}]_{\times}^2 \\
% &= \bm{1} - \alpha_2 \bm{\theta}^{\times} + \alpha_3 \left(\bm{\theta}^{\times}\right)^2 \\
% &= \left[\operatorname{d_{\scriptscriptstyle{L}}Exp}_{\bm{\theta}}\right]^{-1}\\
\end{aligned}
\end{equation*}
This formula has previously been derived by \cite{cardona1988beam}.

\hypertarget{ddLogSO3}{%
  \subsubsection{\texorpdfstring{\(\operatorname{d^2_{\scriptscriptstyle{LR}}Log}\): %
Derivative of the Left Differential of the Logarithm}{%
ddLogSO3: Derivative of the Left Differential of the Logarithm}}\label{sec:ddLogSO3}}
%
\paragraph*{Definition}
The derivative \(\operatorname{d^2_{\scriptscriptstyle{LR}}Log}\left(\bm{\theta}; \, \bm{p}\right)\) arises when a logarithmic parameterization is linearized:
%
\begin{equation*} 
\begin{aligned}
\operatorname{d^2_{\scriptscriptstyle{LR}}Log}\left(\bm{\theta}; \, \bm{p}\right)\bm{u}
&= \left.\frac{\mathrm{d}}{\mathrm{d} \varepsilon} \operatorname{d_{\scriptscriptstyle{L}}Log} \left(\operatorname{Log}\left(\operatorname{Exp}(\varepsilon \bm{u})\operatorname{Exp}(\bm{\theta})\right) \right)  \bm{p} \,\right|_{\varepsilon=0} \\
%
&=\partial_{\bm{\theta}}\operatorname{d_{\scriptscriptstyle{L}}Log}\left({\bm{\theta}}; \, \bm{p}\right) 
  \left.\frac{\mathrm{d}}{\mathrm{d}\varepsilon} \operatorname{Log} \left( \operatorname{Exp} (\varepsilon \bm{u}) \operatorname{Exp} \bm{\theta}\right) \right|_{\varepsilon=0} \\
%
  &= \partial_{\bm{\theta}}\operatorname{d_{\scriptscriptstyle{L}}Log}\left({\bm{\theta}}; \, \bm{p}\right)\, \operatorname{d_{\scriptscriptstyle{R}}Log}\left({\bm{\theta}}\right)  \bm{u} \\
\end{aligned}
\end{equation*}
where \(\operatorname{d_{\scriptscriptstyle{R}}Log} {\bm{\theta}}\) is given by Equation~(\ref{eq:drLogSO3}) and :
\begin{equation*}
\partial_{\bm{\theta}} \operatorname{d_{\scriptscriptstyle{L}}Log}\left({\bm{\theta}}; \, \bm{p}\right) \, \bm{u}
= \left.\frac{\mathrm{d}}{\mathrm{d}\varepsilon}\operatorname{d_{\scriptscriptstyle{L}}Log}\left({\bm{\theta}+\varepsilon \bm{u}};\,  \bm{p}\right)\right|_{\varepsilon=0}
\end{equation*}

%
\paragraph*{Expression}
%
In terms of the coefficients \(\alpha_i\) and \(\beta_i\) in
Equation~(\ref{eq:ber}) the function takes the following form:
%
\begin{equation}
\begin{aligned}
\operatorname{d^2_{\scriptscriptstyle{LR}}Log}(\bm{\theta}; \, \bm{p}) 
&=\biggl(\alpha_2 {\bm{p}}^\times
       +\alpha_3\bigl(({\bm{\theta}}\cdot  \bm{p}) \bm{1}+{\bm{\theta}} \otimes \bm{p}-2 \bm{p}\otimes \bm{\theta} \bigr)
+\beta_2 \left({\bm{\theta}}^\times\right)^2  \bm{p}\otimes \bm{\theta}\biggr) \operatorname{d_{\scriptscriptstyle{R}}Log}{\bm{\theta}}
\end{aligned}
\label{eq:ddLogSO3}
\end{equation}
%
where \(\operatorname{d_{\scriptscriptstyle{R}}Log} {\bm{\theta}}\) is given by Equation~(\ref{eq:drLogSO3}).
This formula has been derived in \cite{nour-omid1991finite}.
%
% \hypertarget{identities-1}{%
% \subsection{Identities}\label{identities-1}}

\hypertarget{rotation-tangents}{%
\subsection{Rotation Tangents}\label{rotation-tangents}}

Consider a vector function \(\bm{q}\) that follows a vector \(\bm{p}\)
as it is rotated by a time-varying rotation
\(\bm{\Lambda}_{\varepsilon}\): %
\begin{equation*} 
\bm{q} \triangleq \bm{\Lambda}_{\varepsilon}\,\bm{p}.
\end{equation*}
%
% \hypertarget{left-tangent}{%
% \subsubsection{Left Tangent}\label{left-tangent}}
A tangent in the right representation is furnished by:
\begin{equation}
\begin{aligned}
\operatorname{d_{L}}(\bm{\Lambda} \bm{p})\cdot\bm{u}&=\lim _{\varepsilon \rightarrow 0} \frac{\operatorname{Exp} \left(\varepsilon \bm{u}\right) \bm{\Lambda} \bm{p}-\bm{\Lambda} \bm{p}}{\varepsilon} \\
%& \approx \lim _{\varepsilon \rightarrow 0} \frac{\left(\bm{1}+\varepsilon \mathbf{e}_i^{\times}\right) \bm{\Lambda} \bm{v}-\bm{\Lambda} \bm{v}}{\varepsilon}\\
&=-(\bm{\Lambda} \bm{p})^{\times} \bm{u}.
\end{aligned}
\label{eq:LeftTangSO3}
\end{equation}

% \FCF{looks a bit strange with a single subsection and formula, suggesting that there is more to come, but we ran out of time; is it better to write it as a sentence? "The left tangent of the vector q is...}

% \clearpage
