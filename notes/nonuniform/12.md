
$$
\newcommand{\FrameBasis}{\mathbf{i}}
\newcommand{\WarpSV}{\varphi}
\newcommand{\WarpFA}{\psi}
\newcommand{\Twist}{\varkappa}
\newcommand{\PlanePosition}{\rho}
\newcommand{\Jv}{J_v}
\newcommand{\Ja}{J_a}
\newcommand{\Jw}{J_w}
\newcommand{\Js}{J_a}
\newcommand{\resid}{g}
\newcommand{\StressVector}{\bm{\sigma}}
\newcommand{\EnhanParam}{\eta}
\newcommand{\EnhanConstFA}{c}
\newcommand{\EnhanShape}{\bar{\bm{A}}}
\newcommand{\EnhanShapes}{\bar{\bm{A}}}
$$

You’re on the right track. The clean way to “tie it up” is to make explicit what your Chapter 1 framework **does** and **does not** determine, and then show how the torsion/warping specialization reduces the freedom to (at most) a **choice of subspace**.

Below is a narrative that is faithful to your framework and answers the DOF question precisely.

## 1) What Chapter 1 actually fixes

Chapter 1 gives the enhanced strain form

$$\bar\varepsilon(\bm r;x)=\hat\varepsilon(\bm e;x)+\EnhanShapes(\bm r)\,\eta(x),$$

with $\eta(x)$ determined locally from the stationarity/orthogonality condition

$$\resid(\eta)=\int_A \EnhanShapes^{\top}\StressVector\big(\hat\varepsilon+\EnhanShapes\eta\big)\,dA=0.$$

Two key consequences (no additional assumptions):

### (ii) **There is an intrinsic scaling gauge for $n_a=1$**

For one enhanced parameter,

$$\EnhanShapes\mapsto \lambda\,\EnhanShapes,\qquad \eta\mapsto \eta/\lambda$$

produces the same enhanced strain field. So any pair of scalars of the form “$\bar\eta$ times shape” is **never** physically identifiable; only the product $\EnhanShapes\eta$ is.

**Answer to your DOF question at the Chapter‑1 level:**

For a chosen dimension $n_a$, the “modeling choice” is the **selection of an $n_a$-dimensional subspace**

$$\tilde{\mathscr E}=\{\EnhanShapes(\cdot)\eta:\eta\in\mathbb{R}^{n_a}\}
\subset L_2(A;\mathbb{R}^3)$$

subject to stability $\tilde{\mathscr E}\cap A[\mathcal E]=\{0\}$.

Everything else (basis, scaling) is gauge.

So: **infinite** freedom in principle (choice of subspace), but **no** freedom in the choice of basis once the subspace is fixed.

## 2) Specialization: torsion/warping identifies a 2D “violation” in the dual

In the prismatic, linear, isotropic specialization (allowing $E(\bm r),G(\bm r)$ varying over $A$ but not in $x$), the sectional $z$-equilibrium weak form reduces to the statement that the shear part must reproduce a functional involving $\partial_x\sigma_{zz}$.

With your torsion strain structure, the equilibrium residual takes the form

$$(\alpha-\Twist)\,L_1(v)+\alpha''\,L_2(v)=0 \qquad \forall v\in H^1(A)/\mathbb{R},$$

where the two functionals are (up to the precise sign conventions you’ve already fixed)

$$L_1(v)=\int_A G\,\nabla\WarpSV\cdot\nabla v\,dA,\qquad
L_2(v)=\int_A E\,\WarpSV\,v\,dA.$$

By construction, $L_1$ is represented by $\WarpSV$ in the $G$-metric; and $L_2$ is represented by $\WarpFA$ via your Riesz relation (your $J$-identities encode the same fact):

$$\int_A G\,\nabla\WarpFA\cdot\nabla v=\int_A E\,\WarpSV\,v.$$

So the “violation space” in the dual is generically 2D:

$$\operatorname{span}\{L_1,\ L_2\}\subset (H^1/\mathbb{R})^*,$$

and the canonical compatible representer space on the primal side is

$$\mathcal S:=\operatorname{span}\{\nabla\WarpSV,\ \nabla\WarpFA\}\subset L_2(A;\mathbb{R}^2).$$

This is the mathematically sharp way to say “the two functionals are associated with $\WarpSV$ and $\WarpFA$.”

## 3) Now the critical point: stability + “one local parameter” forces a 1D choice inside $\mathcal S$

Here is where your Chapter‑1 **stability** condition matters and gives you the cleanest thesis narrative.

In this chapter you are *not* in the “no warping DOFs” setting: your compatible strain already contains the global warping measure $\alpha$ multiplying the SV shear field $\nabla\WarpSV$. That means the compatible strain space $A[\mathcal E]$ already contains the direction $\nabla\WarpSV$.

Therefore, if you took an enhanced mode whose span contains $\nabla\WarpSV$, the enhanced space would intersect $A[\mathcal E]$ nontrivially and your local condensation would destroy identifiability / coercivity (this is exactly the pathology stability is preventing).

So with your *chosen underlying beam kinematics*, the enhancement cannot “just add $\nabla\WarpSV$” and it also cannot take $\tilde{\mathscr E}=\mathcal S$, because $\mathcal S$ contains $\nabla\WarpSV$.

If you insist on **one** local parameter ($n_a=1$) *and* you want to stay inside the physically motivated 2D representer family $\mathcal S$, then the most general stable choice is precisely:

$$\boxed{\EnhanShapes_c(\bm r)\ \propto\ \nabla(\WarpSV+c\,\WarpFA),\qquad c\neq 0.}$$

- $c\neq 0$ is not cosmetic: $c=0$ drops you back into the compatible subspace and violates stability (and you already saw it degenerates the coupling).

This gives you the clean thesis sentence you want:

“Chapter 1 reduces the modeling choice to the selection of an enhancement subspace. In the torsion/warping specialization, equilibrium identifies a 2D canonical representer space $\mathcal S=\operatorname{span}\{\nabla\WarpSV,\nabla\WarpFA\}$. Because $\nabla\WarpSV\in A[\mathcal E]$, stability prohibits taking $\mathcal S$ itself; with one condensed parameter the admissible enhancements are exactly the 1D complements of $A[\mathcal E]$ inside $\mathcal S$, parameterized by $c\neq 0$.”

That’s the structural reason you “must” see a *linear combination of two gradients* when you insist on a single enhanced scalar.

## 4) How many modeling DOFs remain, and how to remove the $\bar\eta$/$c$ redundancy cleanly

### If $n_a=1$ (your present chapter)

- **Modeling DOF:** exactly **one** (the direction $c$, modulo reparameterizations induced by basis/scaling conventions).
- **$\bar\eta$ is not a modeling DOF.** It is either:

- (a) the scalar output of the local solve for $\eta(x)$ given your chosen $\EnhanShapes_c$, or
- (b) a normalization factor you absorb into the definition of $\EnhanShapes_c$.

Concretely, your final strain term looks like

$$(\Twist-\alpha)\,\bar\eta(c)\,\nabla(\WarpSV+c\,\WarpFA).$$

Because of the one‑mode scaling gauge, you can present this without any redundancy in either of two equivalent ways:

- **Option A (keep $c$ explicit, absorb $\bar\eta$ into the parameter):**

$$\EnhanShapes_c=\nabla(\WarpSV+c\,\WarpFA),\qquad \eta(x)=(\Twist-\alpha)\bar\eta(c).$$
- **Option B (fix the parameter to $\Twist-\alpha$, absorb $\bar\eta$ into the shape):**

$$\EnhanShapes_c=\bar\eta(c)\,\nabla(\WarpSV+c\,\WarpFA),\qquad \eta(x)=\Twist-\alpha.$$

Pick one convention and state it explicitly; then only $c$ remains as an actual modeling knob.

### If you allowed $n_a=2$

At the Chapter‑1 level, with $n_a=2$ you could in principle eliminate the “direction choice” entirely (no $c$) by letting the enhancement subspace be 2D.

But **with your current underlying kinematics that already includes $\nabla\WarpSV$**, stability prevents taking any enhanced subspace that contains $\nabla\WarpSV$. So a 2D enhancement *within* $\mathcal S$ is incompatible with your stability requirement unless you restructure the compatible field (e.g., remove $\WarpSV$ from the global warping basis and treat it as enhanced instead). That’s a different modeling decision than “this chapter follows chapter 2.”

So, given your thesis structure, it is coherent to keep $n_a=1$ and accept that one modeling DOF remains.

## 5) Where Armero fits (and what you can safely claim)

Within your framework, **Armero’s value**

$$c=\frac{\Jw}{\Js}=-\frac{(\WarpFA,\WarpSV)_G}{(\WarpFA,\WarpFA)_G}$$

is best presented as the **unique canonical selection rule** that *removes* the remaining one‑mode direction freedom by an extra criterion (not contained in Chapter 1), e.g.:

- $G$-orthogonalization of $\WarpSV+c\WarpFA$ against $\WarpFA$, or equivalently
- minimization of $\|\WarpSV+c\WarpFA\|_G$ over $c$.

That is a clean, non-overstated statement:

“Chapter 1 yields a one‑parameter family of stable one‑mode enhancements inside $\mathcal S$. Armero’s model corresponds to the unique $G$-orthogonal/minimum‑norm choice $c=\Jw/\Js$.”

This supports the claim “we generalize Armero” in a defensible way: you’re not saying *all* $c$ are equally physical; you’re saying the framework exposes a **family**, and Armero is the **distinguished** member selected by an additional variational/optimality principle.

If you want, I can rewrite the above into ~10–15 lines of thesis-ready prose (no extra math) that slots directly after your “two functionals” paragraph, using your exact symbols $\EnhanShapes,\eta,\Jv,\Jw,\Js$.
