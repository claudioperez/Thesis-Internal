

\subsection{Introduction}

% \paragraph{Motivation}

This chapter describes the implementation of the prior developments. 
The work builds on the modular architecture that has been pioneered by authors like  \cite{filippou1996fedeas}, \cite{desouza2000forcebased}, 
\cite{lecorvec2012nonlinear},\cite{scott2008softwarea}. 

Two elements are presented which are supported by four geometric transformation options. 
The cross section can be used with both shear-deformable and shear-free beam elements and support and at least one
auxiliary warping mode.


% \paragraph{Literature}
% \cite{taucer1991fiber}\cite{ngoc2017damage}. 

% \paragraph{Scope.}
% Only shear-deformable formulations are discussed. 
% Shear-deformable beam theories are known to converge to Euler-Bernoulli beam theory in the limit of increasing span-to-depth ratio, and therefore are valid over a larger range of structure types, making them more suitable for the demands of our context.

% For similar reasons, only distributed inelasticity formulations are considered (as opposed to concentrated inelasticity formulations). 
% Concentrated inelasticity formulations inherently carry several restrictive assumptions pertaining to things like element loading (typically assume no distributed loads) and resultant coupling. 

\iffalse
\subsection{Modeling Considerations}
\begin{enumerate}
    \item Geometric nonlinearity. 
    In the context of frame models (Box~\ref{box:model-summary}), geometric nonlinearities arise from two sources:
      \begin{enumerate}
        \item Element geometry.
        \item Section geometry.
      \end{enumerate}
      This distinction is illuminated by considering classical buckling modes:
      \begin{table}[H]
          \centering
          \caption{Buckling modes.}
          \begin{tabular}{llll}
              \toprule
              Mode & Description & Type & Examples \\
              \midrule
              Mode I 
              & Compressive lateral buckling
              & Element & \ref{sec:frame-1002} 
              \\
              
              & Compressive torsional buckling
               & 
               \\ 

              & Compressive lateral-torsional buckling 
              &
              & %\ref{sec:frame-0014}, % C
                %\ref{sec:frame-1028}, 
                 \ref{sec:frame-1030}, % T
                %\ref{sec:frame-1034}
              \\
              \LightRule 
              & Flexural lateral-torsional buckling 
              & Section
              & %\ref{sec:frame-1018}, 
                %\ref{sec:frame-1031},
                \ref{sec:frame-1035}
              \\
              \LightRule 
              & Hockling 
              & Element 
              & \ref{sec:frame-1010}
              \\
               \bottomrule
          \end{tabular}
      \end{table}
    \item \label{item:shear} Boundary flexibility. \\
      Negligible when $L$ is large.
    \item Strain uniformity. 
      Negligible when:
      \begin{enumerate}
          \item[i)] \ref{item:shear} is negligible,
          \item[ii)] 
      \end{enumerate}
    \item Stress coupling. \\
      Negligible when both:
      \begin{enumerate}
        \item[i)] \ref{item:shear} is negligible,
        \item[ii)] $T$ is small.
      \end{enumerate}
\end{enumerate}
\fi


\subsection{Element Library}\label{elements}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.56\linewidth]{Body/ForceFrame.png}
    \resizebox{0.34\linewidth}{!}{\input{Body/Library/element}}
    \caption{Distributed-inelasticity frame element.}
    \label{fig:force-frame}
\end{figure}


% There are six elements in the library, each supporting various options
% for element kinematics. 
% The element kinematic formulations are summarized in \cref{sec:genwarp} and are identified as
% \gls{frame:exact}, %\gls{frame:delta}, \gls{frame:axial}
% or \gls{frame:basic}. 

% All new frame elements will accept the following standard fields:

% \begin{longtable}[]{@{}
%   >{\raggedright\arraybackslash}p{(\columnwidth - 4\tabcolsep) * \real{0.1216}}
%   >{\raggedright\arraybackslash}p{(\columnwidth - 4\tabcolsep) * \real{0.1622}}
%   >{\raggedright\arraybackslash}p{(\columnwidth - 4\tabcolsep) * \real{0.7162}}@{}}
% \toprule
% \begin{minipage}[b]{\linewidth}\raggedright
% Field
% \end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
% Type
% \end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
% Description
% \end{minipage} \\
% \midrule
% \endhead
% \texttt{transform} & Object & Reference to a pre-defined configuration
% transformation \\
% \texttt{section} & Object & Reference to a pre-defined cross section
% model \\
% \texttt{kinematics} & Flag & Optional variable specifying the kinematic
% formulation of the element. \\
% % \texttt{shear} & Flag & Optional flag variable specifying the shear formulation. \\
% \bottomrule
% \end{longtable}

\iffalse
\paragraph{1. PrismFrame}
The \texttt{Prism} element is a standard linearly elastic prismatic
  frame. This element is ideal for modeling simple slender members that
  are not expected to exhibit inelastic response or experience
  excessively large deformations. 
  Nonlinear effects from moderately
  large displacements can be accounted for using the \texttt{PDelta} or
  \texttt{Corotational} geometric transformations. Additionally, the
  \texttt{kinematics} option can be used to incorporate \(P-\delta\)
  effects with \texttt{Axial}  (\(P-\delta\)) element kinematics. 
  This model is inherently elastic. 
  If an inelastic section is supplied through the \texttt{section} argument,
  resultant stresses and stiffness are obtained by linearizing about the
  strain-free state. 
  The implementation of 3D \gls{frame:axial} element
  kinematics is novel and supports asymmetric sections.
\fi

\paragraph{1. ForceFrame}
The \ForceFrame{} element is ideal for modeling inelasticity in
frames \citep{neuenhofer1997evaluation}. 
  
Mixed finite element formulations tend to be desirable when inelastic response must be represented, and geometric nonlinearities are expected to be moderate \citep{neuenhofer1997evaluation}. 
The development and implementation of these methods tends to be more involved than displacement formulations, and they are typically applied to linearized governing equations. 

This element is adapted from the \texttt{forceBeamColumn} and
\texttt{forceBeamColumnCBDI} elements of \OpenSees{} and
\texttt{Dinel3dFrm\_EBwFF} from FEDEASLab. 

Implementation details are discussed in \cref{sec:ForceFrame}.

\iffalse
\paragraph{3. CubicFrame}
  The \texttt{Cubic} element fits displacements to a cubic polynomial
  inside a 2-node basic system with optional shearing deformations. This
  element requires internal iterations in the presence of inelastic
  shear deformations. 
  The implementation of this element derives from
  the \texttt{Dinel3dFrm\_EBwDF} and \texttt{Dinel3dFrm\_TwDF*} elements
  of FEDEASLab \citep{filippou1996fedeas}.
\fi

\paragraph{2. ExactFrame.}
The \ExactFrame{} element is a \(C^0\) displacement formulation of
the geometrically exact shear-deformable Cosserat rod. 
This element is ideal
for modeling large elastic deformations. 
Reduced integration must be
used to avoid shear locking, and consequently a combination of \(h\)
and \(p\) refinement is required in order to adequately capture
inelasticity.


% \subsection{Euler}
%   The \gls{frame:euler} element is a straightforward \(C^1\) displacement
%   formulation. Computations are performed in a local system.
%   \gls{frame:basic} and \gls{frame:axial} element kinematics are supported
%   with arbitrarily many nodes. The implementation of this element is not
%   derived from a preexisting element of \OpenSees{} or FEDEASLab.

% \subsection{Shear}
%   The \gls{frame:shear} element is a straightforward \(C^0\) displacement
%   formulation. Computations are performed in a local system.
%   \gls{frame:basic} and \gls{frame:axial} element kinematics are supported.
%   Reduced integration must be used for this element to avoid shear
%   locking, and consequently a combination of \(h\) and \(p\) refinement
%   is required in order to adequately capture inelasticity.


\subsection{Transformation Library}\label{sec:transformation-types}

\begin{figure}[H]
    \centering
    \resizebox{0.6\linewidth}{!}{\input{Body/Library/transform}}
    \caption{Class diagram for the geometric transformation abstraction.}
    \label{fig:frame-class}
\end{figure}

\begin{enumerate}
    \item\label{lib:T1} \texttt{Linear}
    \item\label{lib:T3} \texttt{Corotational} (\cref{sec:coro-details})
    \item\label{lib:T4} \texttt{Spherical} \cref{sec:sfin}
\end{enumerate}

Available transformations on the rotation parameterization include:
\begin{enumerate}
    \item \texttt{Iter}. This option is the most computationally efficient. 
    It is not vulnerable to singularities and is most suitable for extremely large rotations (Example~\ref{sec:frame-1010}). 
    The formulation exhibits artificial path dependence (Example~\cref{sec:frame-1007}).
    \item \texttt{Incr}. 
    \item \texttt{Init}.
\end{enumerate}

\subsection{Section Library}

\begin{figure}[H]
    \centering
    \resizebox{0.5\linewidth}{!}{\input{Body/Library/section}}
    \caption{Class diagram for the \texttt{ShearFiber} section.}
    \label{fig:section-class}
\end{figure}

The \ShearFiber{} section implements the developments of \cref{sec:gensec} and \cref{sec:timo}. 

\begin{table}[H]
    \caption{Caption}
    \label{tab:ShearFiberInputs}
    \centering
    \begin{tabular}{ll}
    \toprule
        \texttt{Kinematics} & A string naming a kinematic formulation: \\
          & \begin{minipage}[t]{0.8\textwidth}
              \begin{itemize}
                \item \texttt{"Wagner"}: Truncated nonlinear kinematics of \eqref{eq:wagner-gl-strain-pi}.
                \item \texttt{"Linear"}: Linearized kinematics \eqref{eq:linear-gl-strain-pi}.
              \end{itemize}
          \end{minipage} 
        \\ \LightRule
        \texttt{Trace} &  A string indicating a trace formulation. This is used to build the \(\RigidShift\) and \(\ShapeShift\) matrices in \eqref{eq:petrov-section}.
         \\ &
          \begin{minipage}[t]{0.8\textwidth}
             \begin{itemize}
               \item \texttt{"None"}:
               \item \texttt{"Energetic"}: The Class III trace described in \cref{sec:trace-energy}. 
               \item \texttt{"Geometric"}: 
                 The Class II trace described in \cref{sec:trace-cowper}.
              \end{itemize}
            \end{minipage}
        \\ \LightRule
        \texttt{Fibers}  &  Array of fiber data with entries containing fields: \\
        & \begin{minipage}[t]{0.8\textwidth}
          \begin{itemize}
             \item \texttt{Location}: 2D coordinate \(\SectionCoordinate_i\) of the fiber in the section.
             \item \texttt{Area}  : Weight assigned to the fiber for performing quadrature in the section.
             \item \texttt{Shape}  : $n_w\times 3$ array of warping shapes and gradients. 
             \item \texttt{Material}: 
          \end{itemize}
        \end{minipage}
        \\
        \bottomrule
    \end{tabular}
\end{table}


\iffalse
\subsection{Element Loading}\label{sec:loading}
\input{Body/Library/Loading/main}

\subsection{Sensitivity}
\cite{al-aukaily2019corotational}
\fi


\subsection{Verification}
\paragraph{Geometric Transformations.}
Five examples from the literature are used to confirm that the proposed geometric transformation framework
reproduces exactly the results of two seminal studies on geometrically exact formulations \citep{simo1986threedimensional}, \cite{ibrahimbegović1995computational}.
%
The examples go on to showcase the effect of enhancing these formulations with strain-objectivity.

For the planar flexural response of a cantilever beam in  Example~\ref{sec:plane} the enhanced formulations with strain objectivity demonstrate excellent agreement with available analytical solutions and equivalent accuracy and convergence characteristics to existing formulations.

The spatial response of the cantilever beam in Example~\ref{sec:frame-1008}
shows that the proposed formulation with objectivity and a symmetric stiffness matrix maintains the accuracy and convergence properties of the wrapped element under spatial loading and very demanding deformation states.  

The curved cantilever of Example~\ref{sec:frame-1007} illustrates the utility of the modular framework for path-independent problems allowing for various reparameterizations of the nodal variables in connection with the invariance isometry \Spherical{}, while preserving the path-independence with the \texttt{Init} interpolation.

The buckling analysis of a flexible rod under torsion in Example~\ref{sec:frame-1010} shows that the proposed formulation with objectivity and a symmetric stiffness matrix maintains the accuracy and 
convergence properties of the wrapped element even for a deformation history with instability.

\paragraph{Warping Consistency.}

\Cref{sec:frame-0012} presents a numerical example demonstrating exact recovery of 
Saint~Venant solutions through the proposed formulation. 
Behavior under inelastic material response is investigated in \Cref{sec:frame-2007} and \Cref{sec:frame-2021}.

